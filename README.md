# LITEBRITE #

### Prereq's for Dev: ###

*  `npm install -g grunt-cli bower`

### To Dev: ###

*  `bower install`
*  `npm install`
*  `grunt watch`
*  `grunt connect` (in a separate term from watch)
